---
import { HERO_CONTENT } from '../data/hero';
---
<section class="section">
  <div class="container">
    <h1 class="display text-6xl md:text-[110px] font-bold leading-[0.9] tracking-[-0.05em]">{HERO_CONTENT.title}</h1>
    <p class="mt-4 text-base text-neutral-600">{HERO_CONTENT.subtitle}</p>

    <div class="mt-8 space-y-4 text-base leading-6 text-neutral-700 max-w-3xl">
      {HERO_CONTENT.paragraphs.map((paragraph) => (
        <p set:html={paragraph} />
      ))}
      <a class="hover:text-neutral-900 link-external" href="mailto:helpdesk@nextfrontier.hk" target="_blank" rel="noopener noreferrer">Let’s talk.</a>
    </div>
  </div>
</section>

